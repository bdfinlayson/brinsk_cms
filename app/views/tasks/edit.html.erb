<%= simple_form_for (@task) do |f| %>
  <%# <%= render 'shared/error_messages', object: f.object %1> %>
  <%= f.input :name, placeholder: "Name" %>
  <%= f.label :description %>
  <%= f.text_area :description, placeholder: "Describe your task..." %>
  <%= f.input :due, placeholder: "Due" %>
  <%= f.button :submit %>
<% end %>

<%= link_to 'Cancel', :back %>
