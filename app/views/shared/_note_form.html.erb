<%= simple_form_for ([@contact, @contact.notes.build]) do |f| %>
  <%= f.input :subject, placeholder: "Subject" %>
  <%= f.label :content %>
  <%= f.text_area :content, placeholder: "Note content...", class: 'tinymce' %>
  <%= f.button :submit %>
<% end %>

<%= tinymce %>
