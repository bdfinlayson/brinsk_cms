<%= simple_form_for([@contact, @contact.appointments.build]) do |f| %>
  <%= f.input :name, placeholder: "Appointment Name" %>
  <%= f.input :starts_at, placeholder: "Starts At" %>
  <%= f.submit %>
<% end %>
